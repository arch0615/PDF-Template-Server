# Server port (Vercel sets this automatically)
PORT=3001

# Allowed client origins (comma-separated for multiple)
# For production, set to your deployed client URL (e.g., https://your-client.vercel.app)
CLIENT_URL=http://localhost:5174
